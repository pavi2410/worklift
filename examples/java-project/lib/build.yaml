dependencies:
  jar: [compile]

clean: [compile, jar]

targets:
  compile:
    - javac:
        sources: src/com/example/lib/StringUtils.java
        destination: build/classes

  jar:
    - jar:
        from: build/classes
        to: build/string-utils.jar
